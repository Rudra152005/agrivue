version: '3.8'

services:
  backend:
    build: .
    ports:
      - "9090:9090"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"

volumes:
  mongo-data:
  redis-data:
